# JLCircuit Agent 待办清单

更新时间：2026-08-28

本文只记录已经进入目标架构、但当前尚未完成或仅完成 MVP 的事项。当前真实实现边界以 `docs/current-architecture.md` 为准；完成任务时应同时更新该架构文档和本清单。

## 优先级

- **P0**：扩大真实写操作前必须完成，关系到设计正确性和可恢复性。
- **P1**：下一阶段核心能力，完成后可以形成较完整的工程闭环。
- **P2**：平台化和专业能力增强。
- **P3**：规模化、部署和生态能力。

## P0：真实 EDA 能力与安全执行基线

### 1. 扩大真实 EDA API 验证覆盖

- [ ] 建立固定测试原理图，覆盖普通引脚、总线、网络标签、电源符号、跨页网络和悬空端点。
- [ ] 对元件、引脚、导线、网络、选区、页面和 DRC/ERC 的读取结果建立版本兼容测试矩阵。
- [ ] 验证元件移动在复杂折线、总线和网络标签场景下的连接保持行为。
- [ ] 区分 EDA 读取缓存延迟、真实写入失败和视觉刷新延迟，形成明确的状态判定规则。
- [ ] 为真实 EDA 回归保存操作前后结构化快照、DRC/ERC 和截图证据。
- [ ] 明确各项 Pro API 在不同嘉立创 EDA 版本中的可用性和降级路径。

验收标准：固定测试设计可重复运行；每项能力都有成功、失败和不可判定样例；不能只以“API 返回成功”作为完成依据。

### 2. 通用 ChangeSet 执行器与回滚

- [ ] 为每个 `ChangeOperation` 增加 `expectedBefore`、目标对象版本、可逆参数和验证规则。
- [ ] 执行前重新读取旧值，发现设计漂移时拒绝执行并要求重新规划。
- [ ] 支持批次执行、幂等操作 ID、部分失败停止和明确的逐项结果。
- [ ] 建立通用事务/补偿框架，不再由每一种写工具单独实现全部回滚逻辑。
- [ ] 保存操作前快照和回滚制品，支持失败自动回滚及用户主动撤销。
- [ ] 回滚后再次执行结构化检查、DRC/ERC 和视觉验证。
- [ ] 恢复或正式实现受控的 `applyChangeSet`，保持工具 allowlist 和确认令牌校验。

验收标准：批量操作中任一步失败时不会继续破坏设计；可恢复到已知快照；审计能说明执行、补偿和验证的每一步。

### 3. 修改差异预览与回滚界面

- [ ] 确认前展示按元件、网络、坐标和规则组织的结构化差异。
- [ ] 高亮显示受影响对象、风险、连接变化和预期验证项。
- [ ] 支持任务树、批次进度、单项失败和剩余未执行项。
- [ ] 在任务卡片中提供取消、重新规划、重试和回滚入口。
- [ ] 展示操作前后截图、DRC/ERC 差异和无法自动判定的人工检查项。

验收标准：用户无需阅读模型长文本即可知道“将修改什么、为何修改、风险是什么、如何撤销”。

## P1：扩大设计工具覆盖

### 4. 原理图写操作

已完成 Beta 基础链：按官方 Pro API 查询器件库、放置元件，创建导线、总线、矩形、多边形和文本；已接入 ChangeSet 确认、参数校验、图元 ID 读回、统一 DRC/截图验证和本地夹具测试。尚未完成真实 EDA 版本矩阵、通用回滚和以下完整 CRUD 能力。

- [ ] 添加、删除和替换元件。
- [ ] 创建、删除和修改普通导线及折线路径。
- [ ] 创建和修改网络标签、电源符号、端口和跨页连接。
- [ ] 修改元件属性、位号、数值、型号和封装关联。
- [ ] 生成常用模块，如电源、MCU 最小系统和接口保护电路。
- [ ] 支持多元件成组移动、对齐、分布和模块级布局调整。
- [ ] 为每种写工具补充写前条件、回滚和真实 EDA 回归测试。

### 5. PCB、BOM 和规则工具

- [ ] 读取 PCB 图元、层、网络、规则、铺铜和 DRC 详情。
- [ ] 实现受控的器件移动、布线、过孔、差分对和长度约束操作。
- [ ] 实现铺铜重建、规则修改和 DRC 修复建议/执行。
- [ ] 接入嘉立创器件库搜索、库存/价格信息和封装匹配。
- [ ] 生成、校验和比较 BOM，检查缺件、替代料和封装冲突。
- [ ] 生成设计规则和自动检查脚本，并纳入确认与回滚链。

验收标准：每类新工具先以只读分析和差异预览上线，通过固定设计集验证后再开放真实写入。

### 6. 自动视觉可读性评估

- [ ] 从截图中识别元件/文字重叠、标签碰撞、密集交叉、长回路线和功能块边界。
- [ ] 将结构化对象坐标与截图区域建立稳定映射。
- [ ] 对修改前后截图计算可解释的可读性差异，不只让模型自由描述图片。
- [ ] 支持局部放大、多区域截图和整页拼接制品。
- [ ] 把视觉评分与结构化连接检查、DRC/ERC 共同纳入验收规则。

## P1：项目级长期记忆

### 7. 项目语义记忆

- [ ] 单独保存设计约束、器件决策、替代关系、已知问题和未完成事项。
- [ ] 每条记忆记录来源消息/文件、设计版本、生效范围、置信度和失效条件。
- [ ] 只允许将用户确认或工具证据支持的内容升级为长期事实。
- [ ] 检测新设计快照与已有记忆冲突，并要求用户确认更新或失效。
- [ ] 提供记忆查看、搜索、修订、删除和导出界面。
- [ ] 使用语义摘要替代当前纯摘录式较早会话压缩，同时保留可追溯来源。

验收标准：新会话可以恢复项目约束和已确认决策；模型推测不会自动污染项目事实。

### 8. 制品与设计版本

- [ ] 持久化修改前后快照、截图、差异、验证结果和回滚数据。
- [ ] 将任务、ChangeSet、EDA 项目版本和制品关联起来。
- [ ] 定义制品保留、清理、迁移和损坏恢复策略。
- [ ] 支持比较任意两个已保存设计状态。

## P2：Skill、Plugin 和 MCP 增强

### 9. Skill 包生命周期

- [ ] 支持技能包安装、卸载、升级和版本锁定。
- [ ] 支持依赖解析、冲突检测、来源信任和签名验证。
- [ ] 支持热加载/热更新，并在失败时回退到上一版本。
- [ ] 支持全局、项目级和会话级启用范围。
- [ ] 增加技能参数配置界面，不再只依赖静态 `skill.json`。
- [ ] 明确可执行技能代码的隔离模型；在此之前继续禁止技能目录脚本执行。

### 10. MCP/插件运行生命周期

- [ ] 支持 OAuth 交互授权和令牌安全存储/刷新。
- [ ] 支持连接失败后的退避重连和动态 capability 更新。
- [ ] 按兼容需求补充旧 SSE 传输回退。
- [ ] 支持插件自动安装、签名校验、升级和卸载。
- [ ] 为本地插件进程增加独立沙箱、文件/网络权限、CPU/内存和运行时间限制。
- [ ] 为外部 MCP 写工具接入差异预览、确认令牌、通用执行器和回滚链。
- [ ] 评估是否把内置 EDA 工具作为标准 MCP Server 对外提供；明确单连接、权限和确认边界后再决定实现。

验收标准：外部工具不能绕过 Skill 权限、风险分级、用户确认、项目隔离和审计。

## P2：外部知识与专业审查增强

### 11. 检索与文档解析

- [ ] 为扫描版 PDF 和图片页增加 OCR，并保存页码与区域定位。
- [ ] 增加 embedding、向量索引、关键词/向量混合检索和 rerank。
- [ ] 实现网页采集、增量同步、来源更新时间和失效处理。
- [ ] 增强 HTML、表格、章节标题、图注和脚注结构解析。
- [ ] 解析嘉立创专有原理图/PCB 设计格式，或通过受控 EDA 导出格式建立结构化索引。
- [ ] 增加索引版本、重建、备份和迁移策略。

### 12. Datasheet Review 深化

- [ ] 结构化提取电气参数表、引脚表、封装表和典型应用图。
- [ ] 归一化芯片型号、封装、温度等级和订购后缀。
- [ ] 建立文档版本、勘误、厂商来源和发布日期优先级。
- [ ] 将参考电路/参考网表与当前设计自动比对。
- [ ] 建立带来源的器件参数缓存及版本失效规则。
- [ ] 对固定芯片集评测数值、单位、引脚和典型应用事实准确率。

验收标准：任何具体参数都能定位到文档版本、页码/表格或原图区域；冲突资料不会被静默合并。

## P2：编排与模型路由

### 13. Orchestrator 完整化

- [ ] 增加显式意图分类和专业工作流路由，不只依赖技能关键词。
- [ ] 将复杂任务拆成持久化子任务图，记录依赖、状态和验收条件。
- [ ] 支持跨回合自动恢复未完成任务，而不是每次重新创建独立计划。
- [ ] 区分等待用户、等待外部系统、可自动重试和永久阻塞。
- [ ] 为重复操作、模型误判工具可用性和长任务恢复建立统一 Supervisor 策略。

### 14. 多模型路由与降级

基础层已完成：默认后端已迁移到 Codex App Server；Provider 按需子进程池、请求级手动选择、Responses 参数注入、统一流式事件、token 统计和 `(sessionId, providerId)` thread 恢复已经实现。以下仍待完成：

- [ ] 按规划、代码、中文说明、视觉和长上下文任务选择模型。
- [ ] 按成本、延迟、上下文长度、工具能力和图像能力进行路由。
- [ ] 实现供应商错误分类、有限重试、备用模型降级和熔断。
- [ ] 统一不同供应商的 reasoning、tool call、usage 和错误格式。
- [ ] 记录每任务的模型、token、成本、延迟和降级原因。
- [ ] Provider 配置热重载、空闲进程淘汰、进程健康熔断和管理界面。
- [ ] 为不支持图像的当前 Provider 接入独立视觉 Provider，并把有来源的视觉结论返回主 thread。
- [ ] 升级 Codex CLI 时用官方 schema 和真实 App Server smoke test 验证 experimental dynamic tools 兼容性。

验收标准：切换或降级模型不会丢失工具调用、ChangeSet、任务状态和用户可见结果。

## P3：安全、可观测性与部署

### 15. 接入安全和密钥治理

- [ ] 实际启用并校验 Bridge Token，拒绝未知 EDA 客户端。
- [ ] 为全部 HTTP/SSE 管理和执行 API 增加一致的鉴权策略。
- [ ] 实现严格 CORS、用户/角色权限和项目访问控制。
- [ ] 支持 TLS 或受控反向代理，安全开放非回环部署。
- [ ] 将模型和 MCP 密钥迁移到系统凭据库或专用 Secret Store。
- [ ] 增加敏感字段脱敏、日志保留和数据导出/删除策略。
- [ ] 增加防篡改审计、备份和审计查询界面。

### 16. Trace、指标和评测体系

- [ ] 为每个模型请求、工具调用、ChangeSet 和验证步骤生成关联 Trace。
- [ ] 采集成功率、延迟、token、成本、重试、回滚和人工接管指标。
- [ ] 建立固定原理图/PCB 回归集以及可重复的基准运行器。
- [ ] 分别评测结构化正确性、电气正确性、视觉可读性和来源准确性。
- [ ] 在版本发布前自动运行离线回归，并对能力退化设置阈值。
- [ ] 为真实嘉立创 EDA 环境建立人工/半自动验收记录。

### 17. 运行和扩展边界

- [ ] 支持多个 EDA 实例连接、项目路由和明确的活动连接选择。
- [ ] 解决或封装 Node.js 内置 SQLite 的实验性运行提示与兼容风险。
- [ ] 完善数据库版本升级、备份恢复和向后兼容测试。
- [ ] 提供服务健康、Bridge、模型、MCP、资料索引和任务队列的统一诊断页面。

## 建议实施顺序

1. 完成 P0 的真实 EDA 回归基线、通用执行器和差异/回滚界面。
2. 在通用执行器上逐步开放原理图写工具，每增加一种操作都补真实 EDA 验证。
3. 建立项目长期记忆和制品版本，使跨回合任务能够稳定恢复。
4. 扩展 PCB/BOM/规则工具，并接入同一确认、事务和验证链。
5. 并行推进知识库、Datasheet、Skill/MCP 生命周期和多模型路由。
6. 在非回环部署或第三方插件规模化之前完成 P3 安全与评测基线。

## 完成规则

- [ ] 代码、自动化测试和真实 EDA 验证均通过。
- [ ] `README.md`、`docs/current-architecture.md` 和本文件同步更新。
- [ ] 新写工具具有风险等级、schema、确认、审计、回滚和验证。
- [ ] 新知识结论保留来源、版本和定位，不以模型记忆代替证据。
- [ ] 不把“能编译”“API 返回成功”或“DRC 没增加”单独视为功能完成。
